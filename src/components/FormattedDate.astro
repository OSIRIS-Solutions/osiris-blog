---
interface Props {
	date: Date;
	language?: 'de' | 'en';
}

const { date, language = 'de' } = Astro.props;
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString(language === 'en' ? 'en-US' : 'de-DE', {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>
